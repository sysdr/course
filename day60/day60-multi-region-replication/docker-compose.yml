version: "3.8"
services:
  multi-region-app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app
    command: uvicorn src.web.app:app --host 0.0.0.0 --port 8000

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

