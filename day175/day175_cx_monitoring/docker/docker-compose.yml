version: "3.9"
services:
  cx-monitor:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8175:8175"
    environment:
      SIMULATION_EVENTS: "10000"
      SESSION_IDLE_TIMEOUT: "1800"
      METRICS_WINDOW_SEC: "300"
