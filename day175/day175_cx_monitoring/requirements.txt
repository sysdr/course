# Customer Experience Monitoring — Python dependencies (runtime + tests).
# Pin versions for repeatable installs; upgrade intentionally.

fastapi==0.111.0
uvicorn[standard]==0.30.1

# Test stack
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
