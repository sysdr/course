services:
  protobuf-log-system:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../logs:/app/logs
      - ../src:/app/src
    environment:
      - PYTHONPATH=/app
    ports:
      - "5000:5000"
    command: python src/performance_tester.py
